# Agent Drift Detection Baseline Registry
# This file is auto-generated by the agent-drift-scan job.
# Each entry records the baseline hash and sections for an agent definition file.
# When drift is acknowledged, the baseline is updated here.
#
# Format:
# agents:
#   <agent-id>:
#     file: <relative path>
#     hash: <SHA-256 of file content>
#     sections: [list of H2/H3 section headers]
#     acknowledged_at: <ISO timestamp or null>
#     notes: <optional notes when drift was acknowledged>

agents: {}
