{
  "language": "go",
  "server": "gopls",
  "install": "go install golang.org/x/tools/gopls@latest",
  "verify": "gopls version",
  "diagnostics_hook": "#!/bin/bash\nINPUT=$(head -c 65536); FILE=$(printf '%s' \"$INPUT\" | jq -r '.tool_input.file_path // \"\"')\n[[ \"$FILE\" != *.go ]] && { echo '{\"decision\":\"approve\"}'; exit 0; }\nERRORS=$(go vet ./... 2>&1 | head -10)\n[ -n \"$ERRORS\" ] && echo \"$ERRORS\" >&2\necho '{\"decision\":\"approve\"}'",
  "notes": "gopls reads go.mod automatically. Run go vet for diagnostics; gopls LSP is for the IDE layer."
}
