# CC Patterns — Consolidator-Generated

> One-line pointers to reinforced patterns promoted by `memory-consolidator`. Each entry links to a full note in the Obsidian vault.
>
> Consolidator-managed only. User-curated rules live in `cc-always.md`.

<!-- Format: `- **{topic_key}** (seen {N}×) — [[Research/Claude-Code/Patterns/{slug}]] — one-line summary` -->

<!-- Patterns will appear here after the first `/cc-memory consolidate` run. -->
