# Memory Drafts — Awaiting Review

> Candidates the consolidator found but could not promote: conflicts between engram observations, ambiguous topic_keys, or patterns with < 3 reinforcements that may be worth watching.
>
> Review in the Obsidian app or via `/cc-memory review`.

<!-- Format: `## {topic_key}` section with `- observation {N}: ...` bullets and `- conflict: ...` note. -->
