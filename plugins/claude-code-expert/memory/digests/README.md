# Memory Digests

`/cc-memory export [--domain ...]` writes dated digests here. One digest = one markdown file summarizing recent CC-scoped engram activity for human reading.

Digests are the "what happened this week in memory" view. Suitable for weekly review alongside the Obsidian inbox at `Projects/Claude-Code-Inbox/`.
