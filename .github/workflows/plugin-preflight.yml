name: Plugin Preflight

on:
  pull_request:
    paths:
      - 'plugins/**'
      - 'schemas/plugin.schema.json'
      - 'schemas/hooks.schema.json'
      - 'scripts/validate-plugin-schema.mjs'
      - 'scripts/lint-hooks.mjs'
      - 'scripts/generate-plugin-indexes.mjs'
      - 'scripts/profile-plugin-context.mjs'
      - 'scripts/plugin-context-baseline.json'
      - 'package.json'
      - 'pnpm-lock.yaml'
      - '.github/workflows/plugin-preflight.yml'
  push:
    branches:
      - main
    paths:
      - 'plugins/**'
      - 'schemas/plugin.schema.json'
      - 'schemas/hooks.schema.json'
      - 'scripts/validate-plugin-schema.mjs'
      - 'scripts/lint-hooks.mjs'
      - 'scripts/generate-plugin-indexes.mjs'
      - 'scripts/profile-plugin-context.mjs'
      - 'scripts/plugin-context-baseline.json'
      - 'package.json'
      - 'pnpm-lock.yaml'
      - '.github/workflows/plugin-preflight.yml'

jobs:
  plugin-preflight:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: pnpm/action-setup@v4
        with:
          version: 10
      - uses: actions/setup-node@v4
        with:
          node-version: '20'
          cache: pnpm
      - name: Install dependencies
        run: pnpm install --frozen-lockfile
      - name: Validate plugin schemas
        run: pnpm check:plugin-schema
      - name: Lint plugin hooks
        run: pnpm check:hooks
      - name: Validate plugin context entry files
        run: pnpm check:plugin-context
      - name: Validate frontmatter and generated plugin indexes
        run: pnpm check:plugin-indexes
      - name: Profile plugin context footprint
        run: pnpm profile:plugin-context
