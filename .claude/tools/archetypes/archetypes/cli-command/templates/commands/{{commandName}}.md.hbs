---
name: {{commandName}}
description: {{description}}
argument-hint: {{#if (includes arguments "positional")}}[target]{{/if}}{{#if (includes arguments "flags")}} [--verbose] [--dry-run]{{/if}}{{#if (includes arguments "options")}} [--format=<format>]{{/if}}
allowed-tools:
{{#each tools}}
  - {{this}}
{{/each}}
{{#if dangerous}}
dangerous: true
{{/if}}
---

# {{capitalize (replace commandName ":" " ")}}

{{description}}

## Usage

```bash
# Basic usage
/{{commandName}}{{#if (includes arguments "positional")}} <target>{{/if}}

{{#if (includes arguments "flags")}}
# With flags
/{{commandName}} --verbose
/{{commandName}} --dry-run
{{/if}}

{{#if (includes arguments "options")}}
# With options
/{{commandName}} --format={{outputFormat}}
/{{commandName}} --env=production
{{/if}}

{{#if (includes arguments "subcommands")}}
# Subcommands
/{{commandName}} list
/{{commandName}} add <item>
/{{commandName}} remove <item>
{{/if}}
```

{{#if (includes arguments "positional")}}
## Arguments

| Argument | Description | Required |
|----------|-------------|----------|
| `target` | [Description of target] | Yes |
{{/if}}

{{#if (includes arguments "flags")}}
## Flags

| Flag | Description | Default |
|------|-------------|---------|
| `--verbose`, `-v` | Show detailed output | false |
| `--dry-run` | Preview without executing | false |
| `--quiet`, `-q` | Suppress output | false |
{{/if}}

{{#if (includes arguments "options")}}
## Options

| Option | Description | Values | Default |
|--------|-------------|--------|---------|
| `--format` | Output format | {{outputFormat}}, json, table | {{outputFormat}} |
| `--env` | Target environment | dev, staging, prod | dev |
{{/if}}

{{#if (includes arguments "subcommands")}}
## Subcommands

| Subcommand | Description |
|------------|-------------|
| `list` | List all items |
| `add` | Add a new item |
| `remove` | Remove an item |
| `show` | Show item details |
{{/if}}

## Examples

### Example 1: Basic Usage

```bash
/{{commandName}}{{#if (includes arguments "positional")}} target{{/if}}
```

Output:
```
{{#if (eq outputFormat "table")}}
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
   {{capitalize (replace commandName ":" " ")}} Results
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

Column1    Column2    Column3    Status
──────────────────────────────────────────────────────────
Value1     Value2     Value3     ✓
Value4     Value5     Value6     ✓

Total: 2 items
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
{{/if}}
{{#if (eq outputFormat "json")}}
{
  "results": [
    {"column1": "value1", "column2": "value2", "status": "success"},
    {"column1": "value4", "column2": "value5", "status": "success"}
  ],
  "total": 2
}
{{/if}}
{{#if (eq outputFormat "markdown")}}
## Results

| Column1 | Column2 | Status |
|---------|---------|--------|
| Value1  | Value2  | ✓ |
| Value4  | Value5  | ✓ |
{{/if}}
```

### Example 2: With Options

```bash
/{{commandName}}{{#if (includes arguments "positional")}} target{{/if}} --verbose{{#if (includes arguments "options")}} --format=json{{/if}}
```

{{#if dangerous}}
## ⚠️ Warning

This command performs destructive operations. You will be prompted to confirm before execution.

```
Are you sure you want to proceed? This action cannot be undone.
Type 'yes' to confirm:
```
{{/if}}

## Error Handling

| Error | Cause | Resolution |
|-------|-------|------------|
| `Target not found` | Invalid target specified | Verify target exists |
| `Permission denied` | Insufficient permissions | Check access rights |
| `Timeout` | Operation took too long | Retry or increase timeout |

## Exit Codes

| Code | Meaning |
|------|---------|
| 0 | Success |
| 1 | General error |
| 2 | Invalid arguments |
| 3 | Target not found |

## See Also

- [Related command 1]
- [Related command 2]
- [Documentation link]

---

**Category:** {{category}}
**Author:** {{author}}
**Created:** {{date}}
